#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t, n, x, num;
    cin >> t;
    while (t--) {
        cin >> n >> x;
        int arr[n];
        for (int i = 0; i < n; ++i) 
            cin >> arr[i];
        sort()

    }

    return 0;
}